/**
 * @node-xray/types
 *
 * Public type contracts shared by all node-xray packages.
 * This file is the single source of truth for the public API surface.
 *
 * Detailed implementation will land in P1; the stubs here exist so the
 * monorepo can be typechecked and built end-to-end from P0.
 */

export const VERSION = '0.1.0' as const;

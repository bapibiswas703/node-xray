# @node-xray/types

Public type contracts shared by all `node-xray` packages. Zero runtime dependencies.

> Implementation land in P1. This package currently exports a `VERSION` constant so the build chain is testable from P0.
